package tigerZone;

public class tester {
	
	/*
		public static void main(String args[]){
			int pd = 0;
			int ave = 0;
			int wins = 0;
			int ties = 0;
			int losses = 0;
			long startTime = System.currentTimeMillis();
			
			for(int i = 0; i < 100; i++){
				pd = GameLoop.gl();
				if(pd > 0){
					wins++;
				}
				else if(pd == 0){
					ties++;
				}
				else{
					losses++;
				}
				ave = ave + pd;
			}
			double aver = (double)ave/100;
			System.out.println("Point Differential Average: " + aver);
			System.out.println("Number of wins: " + wins);
			System.out.println("Number of ties: " + ties);
			System.out.println("Number of losses: " + losses);
			final long endTime = System.currentTimeMillis();
			long totalTime = endTime - startTime;
			double t = (double)totalTime;
			System.out.println("Total execution time: " + (t/1000) + " seconds or a " + (t/100000) + " seconds per game average");
		}
		
		*/
}
